{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for Weather Dashboard",
  "main": "server.js",
  "scripts": {
  "type": "module",   
    "start": "npm run build && node dist/server.js",
    "dev": "nodemon --watch src --ext ts --exec ts-node src/server.ts",
    "build": "tsc", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "nodemon": "^3.1.0",
    "@types/node": "^20.4.2",
    "@types/express": "^4.17.21"
  }
}

